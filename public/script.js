const q=document.querySelector('#search'),rows=[...document.querySelectorAll('#roster li')],empty=document.querySelector('#empty');
q.addEventListener('input',()=>{const s=q.value.trim().toLowerCase();let n=0;rows.forEach(r=>{const show=!s||r.dataset.agent.includes(s);r.hidden=!show;if(show)n++});empty.hidden=n>0});
const p=document.querySelector('#preview'),d=document.querySelector('#demo');p.addEventListener('click',()=>{const show=d.hasAttribute('hidden');if(show){d.removeAttribute('hidden');p.textContent='Hide ECHO preview'}else{d.setAttribute('hidden','');p.textContent='Preview ECHO'}});
document.querySelector('#form').addEventListener('submit',e=>{e.preventDefault();const f=new FormData(e.currentTarget),body=[`Name: ${f.get('name')}`,`Work email: ${f.get('email')}`,`Business: ${f.get('business')}`,`Website: ${f.get('website')||'Not provided'}`,'','Repeated task:',f.get('task'),'','Desired outcome:',f.get('outcome'),'','Human approver:',f.get('approver')].join('\n');location.href=`mailto:darnleyweekes@prime24ai.com?subject=${encodeURIComponent('ALPHA Agent Force - Private Tryout Request')}&body=${encodeURIComponent(body)}`});